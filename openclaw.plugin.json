{
  "id": "agent-manager",
  "name": "Agent Manager",
  "description": "Tools for managing tasks, cron jobs, contexts, integrations, Gmail, Google Calendar, and the Garage feed via the Agent Manager API.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Agent Manager API base URL (default: http://localhost:8000/api)"
      },
      "productType": {
        "type": "string",
        "enum": ["garage", "network_chain"],
        "description": "Product type — controls which tools are registered (default: garage)"
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "API Base URL",
      "placeholder": "http://localhost:8000/api"
    }
  }
}